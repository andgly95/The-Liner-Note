{
  "name": "discogs-mcp-server",
  "version": "1.0.0",
  "description": "MCP server exposing the Discogs API: search, releases, artists, labels, collections, and wantlists",
  "private": true,
  "type": "module",
  "bin": {
    "discogs-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
